import AccountDirectory from "@/components/admin/account-directory";

export default function AdminFacultyPage() {
  return (
    <AccountDirectory
      initialRole="faculty"
      lockRole
      title="Faculty Management"
      description="Review faculty registrations, approve pending accounts and manage faculty access."
    />
  );
}
